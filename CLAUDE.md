# EUKoreanLeager

UEFA 협회계수 TOP 10 리그에서 뛰는 한국 선수의 시즌별·대회별 기록 대시보드.

## 다음 세션 시작 시 확인 (2026년 8월 20일 세션 종료 시점 기준)

- ~~배경색 등 테마/디자인 수정~~ → **완료.** 라이트/다크 토글 추가함
- ~~DB 연동~~ → **완료 (2026년 8월 20일).** `PLAYERS`가 이제 Supabase에서 온다. 아래 "Supabase DB 연동" 섹션 참고. 사용자가 브라우저에서 로그인→편집→저장→삭제까지 실제로 테스트 완료, `updated_at` 자동 갱신 트리거도 추가·확인 완료
- **GitHub Pages 배포 진행 중, 완료 여부 미확인** — 세션 종료 시점에 사용자가 Settings→Pages에서 "Deploy from a branch"(main, `/root`) 설정하는 중이었음(커스텀 도메인은 안 씀, 비워둠). **다음 세션 시작하면 먼저 `https://fastwon.github.io/KorFootyEurope/` 접속되는지, 공개 상태에서 읽기는 되고 로그아웃 상태라 편집 버튼은 안 보이는지 확인할 것**
- 그 외 로드맵: 3단계(24시간마다 AI API로 최신 기록 자동 반영하는 Supabase Edge Function + 크론) 착수 전. "26/27 진행 상황" 섹션의 "남은 단계"·"미확정 → null 유지"도 계속 참조 (황희찬 행선지, 리그 개막 후 실제 기록 채우기 등)

## 프로젝트 형태

- **`koreaFootball.html`이 프론트엔드 전부다.** HTML·CSS·JS가 한 파일에 인라인돼 있고 빌드·번들러 없음. **다만 선수 기록(`PLAYERS`)은 2026년 8월 20일부로 하드코딩이 아니라 Supabase DB에서 매 로드마다 fetch해온다** — 아래 "Supabase DB 연동" 섹션 참고. `UEFA_LEAGUES`·`COUNTRY_COLOR`·`COMP_COUNTRY`·`LC`·`SEASONS`·`SL`·`CUR`·`ASOF` 같은 구조적 상수는 여전히 파일에 하드코딩돼 있다.
- git 저장소다 (2026년 8월 20일부로 GitHub `fastwon/KorFootyEurope`, `main` 브랜치). 커밋은 사용자가 명시적으로 요청할 때만 한다.
- `index.html`은 GitHub Pages용 리다이렉트 스텁이다 — 실제 페이지는 여전히 `koreaFootball.html`. GitHub Pages가 루트의 `index.html`을 기본으로 찾기 때문에 존재. 배포 URL: `https://fastwon.github.io/KorFootyEurope/`
- 과거 스냅샷들은 git 이전 시절의 "복사로 되돌리기" 관행 흔적이라 `.gitignore` 처리돼 로컬에만 남아있고 저장소엔 없다. 최신순: `koreaFootball.pre-db.html`(DB 연동 직전, 2026-08-20) → `koreaFootball.pre-editui.html`(편집 UI 도입 직전, 2026-08-18) → `koreaFootball.backup.html`(기록 검증·구간 구조 도입 이전, 오래됨 — 되돌릴 때 쓰지 말 것) → `koreaFootball.2526final.html`(1단계 시즌축 추가 직전)

## Supabase DB 연동 (2026년 8월 20일 추가, DB 로드맵 2단계)

**3단계 로드맵**: 1) 프론트단 편집 UI(완료, 2026-08-18) → 2) DB 연동(완료, 2026-08-20) → 3) 백엔드 + AI API로 24시간마다 최신 기록 자동 반영(착수 전). 계획 파일: `~/.claude/plans/snappy-coalescing-river.md`.

- **프로젝트**: Supabase, ref `pkrqyawldxdpvcncjtdc`. URL·anon key는 `koreaFootball.html`의 `SUPABASE_URL`/`SUPABASE_ANON_KEY` 상수에 그대로 박혀 있다 — **이건 의도된 것**. anon key는 Supabase 설계상 공개돼도 되는 키이고(비밀 유지 대상이 아님), 실제 방어선은 RLS 정책이다. **`service_role` 키는 절대 이 파일이나 어떤 클라이언트 코드에도 넣으면 안 됨** — 그건 3단계 서버측 Edge Function에서만 쓴다.
- **테이블 `players`**: `id`(uuid, PK) / `name` / `en` / `pos` / `ava_bg` / `ava_c` / `seasons`(jsonb) / `created_at` / `updated_at`. `seasons` 컬럼에 기존 JS 데이터 모델(`{sid: null|{stints:[...]}}`)을 그대로 JSONB로 저장 — 완전 정규화 대신 이 방식을 택한 이유는 `getPlayerData`/`stintsOf`/`renderCards` 등 기존 렌더링 로직이 정확히 이 중첩 구조를 전제로 짜여 있어서, DB row를 `rowToPlayer()`로 매핑만 하면 **기존 렌더링 로직을 한 줄도 안 건드리고** 그대로 재사용할 수 있기 때문.
  - **`updated_at` 자동 갱신 트리거 추가함(2026년 8월 20일)** — `default now()`는 INSERT 시점에만 값을 채우고 UPDATE 때는 그대로 안 바뀌길래, `set_updated_at()` 함수 + `players_set_updated_at` BEFORE UPDATE 트리거를 SQL Editor에서 추가 실행했다(`fix_updated_at_trigger.sql`, 1회성이라 `.gitignore` 처리). 이제부터 매 UPDATE마다 `updated_at`이 자동으로 현재 시각으로 바뀐다.
- **RLS 정책**: 공개 SELECT(`using(true)`), INSERT/UPDATE/DELETE는 `auth.role()='authenticated'`만 허용. 특정 user_id 소유권 체크는 안 함 — 어차피 로그인 계정이 사용자 본인 1명뿐이라 "로그인했으면 다 허용"으로 단순화.
- **로그인**: Supabase Auth 이메일/비밀번호. 헤더의 `🔐 로그인` 버튼 → 모달(`#login-overlay`, 편집 모달과는 별개) → `signInWithPassword()`. 세션은 Supabase 클라이언트가 알아서 localStorage에 유지한다. `sb.auth.onAuthStateChange`로 세션 상태를 `body.logged-in` 클래스에 반영 — `✏️ 편집 모드` 버튼 자체가 이 클래스가 있을 때만 보인다(`body.logged-in .edit-toggle-btn`). 로그아웃하면 편집모드도 강제로 꺼짐(`setLoggedInUI`).
- **데이터 흐름**: 페이지 로드 시 `loadPlayers()`가 `PLAYERS`를 채우고 나서야 `render()`가 실행된다(`loadPlayers().then(render)`) — 예전처럼 `PLAYERS`가 소스에 상수로 박혀있지 않으므로 순서가 중요하다. 저장은 `savePlayerFromModal()`이 `sb.from('players').upsert(...)`로 그 선수 1명만 즉시 DB에 반영 — **더 이상 "파일에 저장" 개념이 없다.** 삭제도 `deletePlayerFromModal()`이 즉시 DB에서 지운다.
- **제거된 것 (편집 UI 1단계에서 쓰던 파일-저장 방식)**: `serializePlayersArray`/`serializePlayer`/`serializeStint`/`serializeSeason`/`serializeComp`/`jsStr`/`buildNewFileHtml`/`saveToFile`/`fileHandle`/`dirty`·`markDirty`·`markSaved`/`beforeunload` 경고/`💾 파일에 저장` 버튼 전부 삭제. File System Access API를 이제 안 쓴다.
- **마이그레이션**: 기존 15명을 `migrate_players.sql`(1회성, `.gitignore` 처리됨)로 Supabase SQL Editor에서 일괄 삽입. 앞으로 새 시즌·선수 추가는 이 파일이 아니라 편집 UI(로그인 후)나 Supabase 대시보드에서 직접 한다.
- **검증 완료**: Node에서 실제 프로젝트에 REST API로 직접 fetch해서 (1) anon key로 읽기 성공 (2) 로그인 없이 쓰기 시도하면 RLS가 401로 막는 것 (3) `loadPlayers()`→`render()` 전체 파이프라인이 실제 DB의 15명 데이터로 6개 시즌×10개 리그탭 전부 정상 렌더링되는 것까지 확인. **아직 브라우저에서 실제 로그인→편집→저장 흐름은 사용자가 직접 테스트 안 함** — 다음에 확인 필요.

## 편집 UI (2026년 8월 18일 추가, 2026년 8월 20일 저장 방식 변경)

- **`✏️ 편집 모드` 토글** — 로그인 상태일 때만 버튼이 보인다(위 참고). 켜면 각 카드에 편집 버튼, `+ 새 선수 추가` 버튼이 나타난다 (`body.edit-mode` CSS로 제어)
- **편집 모달** — 이름·영문명·포지션·아바타 색상 + 시즌 탭(6개) × 구간(stint) × 대회(comp) 중첩 편집. 리그는 `UEFA_LEAGUES` 기준 `<select>`라 오타로 깨질 수 없음. 대회명은 `<datalist>`로 기존 등록된 이름 자동완성 제공, **`COMP_COUNTRY`에 없는 대회명을 입력하면 경고 문구**가 뜬다(회색 배지로 표시된다는 안내) — 저장을 막지는 않음. 이 부분은 2026-08-18 도입 이후 변경 없음.
- **저장은 이제 즉시 DB 반영** — 위 "Supabase DB 연동" 참고. `UEFA_LEAGUES`·`COUNTRY_COLOR`·`COMP_COUNTRY`·`LC` 같은 구조적 상수는 여전히 **이 UI로 편집 불가** — 새 리그 추가 등은 여전히 수동으로 파일 안 4곳 동기화해야 한다(위 "리그·색상 상수의 동기화" 참조)

## 라이트/다크 테마 토글 (2026년 8월 18일 추가)

- 헤더 툴바의 🌙/☀️ 버튼으로 전환, `localStorage`(`euko-theme` 키)에 저장돼 다음 방문에도 유지된다
- CSS는 전부 `var(--토큰)` 기반. `:root`에 다크 팔레트(GitHub 다크 그대로: `#0d1117`/`#161b22`/`#30363d`/`#58a6ff` 등)를, `:root[data-theme="light"]`에 라이트 팔레트(GitHub 라이트 기준: `#ffffff`/`#f6f8fa`/`#d0d7de`/`#0969da` 등)를 정의
- **기본 테마는 라이트.** `localStorage`에 저장된 값이 없으면(`euko-theme` 키 미설정) 라이트로 시작 — 2026년 8월 18일 변경 (원래는 다크가 기본이었음)
- **인라인 `style=`로 직접 찍는 배지/아바타/순위칩도 테마에 맞춰 반전된다.** 리그 배지(`LC`)·선수 아바타(`ava.bg`/`ava.c`)·계수 탭 순위 칩(`rankBgs`/`rankColors`)은 전부 "짙은 배경 + 밝은 글자" 조합으로 다크 전제로 설계돼 있었는데, 라이트에서 그대로 쓰면 새까만 배지가 흰 배경 위에 붕 떠 보이는 문제가 있었다(2026년 8월 18일 발견·수정). `chip(bg,tc)` 헬퍼(compColor 옆에 정의)가 라이트일 때 배경↔글자를 맞바꿔서, 원래 짙은 배경이 짙은 글자가 되고 밝은 글자가 옅은 배경이 되는 방식으로 해결 — 별도의 라이트용 색상 데이터를 안 만들어도 색감이 그대로 유지된다. `toggleTheme()`이 `render()`·`renderCoef()`를 다시 호출해야 이 인라인 색들이 갱신되므로, 토글 함수를 건드릴 땐 이 재호출을 빠뜨리지 말 것
  - `COUNTRY_COLOR`·`compColor()`가 반환하는 대회 점(dot) 색은 **그대로 둠** — 점은 배경 없이 단색 원 하나라 페이지 배경이 바뀌어도 그 자체로 잘 읽힌다는 판단. 문제 생기면 그때 손볼 것
  - 편집 모달의 아바타 미리보기·컬러피커는 **원본(다크 기준) 색을 그대로 보여준다** — `chip()` 반전은 카드 렌더링에만 적용, 모달은 "저장되는 실제 값"을 그대로 보여주는 게 맞다고 판단
- 초기화 스크립트(`data-theme` 속성 설정)는 `<script id="mainscript">` 맨 앞, `UEFA_LEAGUES` 정의보다도 위에 있다 — 페이지 로드 시 테마가 늦게 적용되며 다크→라이트로 깜빡이는 걸 막기 위함(FOUC 방지)
- 저장(`buildNewFileHtml`)은 이 토글 상태를 정규화하지 않는다 — 어차피 다음 로드 때 `localStorage` 기준으로 다시 설정되므로 저장 파일에 어떤 테마가 찍혀 있어도 무해함

## 파일 구조

| 대역 | 내용 |
|---|---|
| `<style>` | 전 스타일. CSS 변수 기반 라이트/다크 테마(위 참고). 다크가 기본값이며 GitHub 다크 테마 계열(`#0d1117` 배경 등)을 그대로 씀 |
| `<body>` | 상단 탭 2개(선수 기록 / UEFA 리그 계수), 시즌 필터, 요약 4칸, 리그 탭, `#content` |
| `<script>` 상수부 | `UEFA_LEAGUES` → `COUNTRY_COLOR` → `COMP_COUNTRY` → `INTL_COLOR` → `LC` → `SEASONS` / `SL` → `let PLAYERS=[]`(Supabase에서 `loadPlayers()`로 채워짐, 하드코딩 아님) |
| `<script>` 로직부 | 이벤트 핸들러 → 조회 헬퍼 → `getPlayerData` → 렌더러 |

## 핵심 데이터 모델: 구간(stint)

**한 시즌은 소속팀 구간의 배열이다.** 시즌 중 이적하면 구간이 2개 이상이 되고, 리그 필터와 모든 집계가 이 단위로 이뤄진다.

```js
'2526':{stints:[
  {league:'리그앙',        team:'FC 낭트',        comps:[{name:'리그앙',apps:6,goals:0,assists:0}]},
  {league:'주필러프로리그', team:'KAA 헨트(임대)', comps:[{name:'주필러프로리그',apps:19,goals:1,assists:0}]}
]}
```

규칙:

- 그 시즌 TOP 10 리그에서 안 뛰었으면 `null` (예: 손흥민 `'2526':null` — MLS 소속). 리그 강등·2부 임대도 같은 처리.
- **소속은 확정됐지만 기록이 아직 없으면 `comps:[]`** 로 둔다. 카드에 "소속만 확정 · 기록 입력 전"이 표시되고 합계 행은 숨겨진다. 리그 탭·계수 탭 인원수에는 정상 반영된다.
- 구간은 **시간 순서대로** 넣는다. 마지막 구간이 그 시즌의 "최종 소속"으로 쓰인다.
- `league`는 반드시 `UEFA_LEAGUES`의 `key`와 문자열이 일치해야 한다. 안 맞으면 그 선수는 어느 리그 탭에도 안 잡힌다.
- 유럽대항전(챔스·유로파·컨퍼런스)과 자국 컵은 **해당 클럽 구간 안에** 넣는다. 오현규의 유로파리그 10경기는 헹크 구간이므로 벨기에 탭에만 잡힌다.
- 대회 우승은 `win:true` 플래그. 표에 🏆로 렌더된다. 기간 합산 모드에서는 시즌 수만큼 누적돼 트로피가 여러 개 찍힌다.
- 단일 구간 시즌도 `stints` 배열로 감싼다. 예외 없음.

## 리그·색상 상수의 동기화

리그를 추가·교체할 때 **네 곳을 함께 고쳐야** 색이 깨지지 않는다.

1. `UEFA_LEAGUES` — 순위·계수·리그 색(`color`)·`key`
2. `COUNTRY_COLOR` — 나라별 색. 자국 대회 배지 색의 근거
3. `COMP_COUNTRY` — 대회명 → 나라 매핑. **새 리그를 넣으면 그 나라 컵 대회 이름도 여기 등록**해야 회색으로 안 빠진다
4. `LC` — 카드 헤더 리그 뱃지의 배경/글자색

`compColor(name)`의 우선순위: `INTL_COLOR`(국제대회) → `COMP_COUNTRY`+`COUNTRY_COLOR`(자국대회) → 회색 `#8b949e`. 회색으로 보이면 3번에 등록이 빠진 것이다.

**편집 모달의 대회명 자동완성**(`compNamesForLeague()`)도 `COMP_COUNTRY`를 그대로 참조한다 — 그 구간의 리그와 같은 나라(`COMP_COUNTRY` 값)인 대회 + `INTL_COLOR`(국제대항전, 항상 포함)만 걸러서 보여준다. 그래서 `COMP_COUNTRY`에 등록이 빠지면 배지 색만 회색으로 뜨는 게 아니라 **자동완성 목록에도 아예 안 뜬다.**

**2026년 8월 20일, 슈퍼컵류 8개 대회 일괄 추가** (사용자 제보로 스페인 수페르코파 누락 발견 → 전체 리그 점검): `수페르코파`(스페인)·`수페르코파이탈리아나`(이탈리아)·`요한크루이프샬`(네덜란드)·`수페르타사`+`타사다리가`(포르투갈, 슈퍼컵+리그컵 둘 다 없었음)·`벨기에슈퍼컵`·`튀르키예슈퍼컵`·`체코컵`(체코는 자국컵 자체가 통째로 없었음)·`FA커뮤니티실드`(잉글랜드). 독일(슈퍼컵)·프랑스(트로페데샹피옹)는 이미 등록돼 있어서 제외. 체코 자체 슈퍼컵은 2015년 폐지, 후속 체코-슬로바키아 슈퍼컵도 2020년부로 없어져서 **추가 안 함**(체코는 리그+컵 2개만).

## 렌더링 파이프라인

```
render()  →  buildLeagueTabs()  →  renderCards()
                                   └ getPlayerData(p)  ← curTab / mode / selS / rfrom·rto 를 읽음
```

- `curRange()` — 단일 모드면 `[selS]`, 합산 모드면 시작~끝 시즌 배열
- `stintsOf(p, sid, league)` — `league`를 주면 그 리그 구간만. 필터링의 단일 진입점
- `leaguesOfPlayer(p)` — `curTab`과 **무관하게** 그 선수가 뛴 리그 전부. 리그 탭 카운트 전용
- `getPlayerData(p)` — `curTab`을 이미 적용해서 반환한다. 호출부에서 다시 필터링하지 말 것
- 반환 형태: `{groups, leagues, primaryLeague, teamLabel, totG, totA, totApp, isRange, seasonRows}`
  - `groups` — 구간별 `{league, team, comps}`. 2개 이상이면 표에 구간 소제목 행(`.stint-row`)이 들어간다
  - `primaryLeague` — 마지막 구간의 리그. 전체 탭에서 카드가 놓일 섹션을 결정
  - 합산 모드에서는 `groups`가 병합된 1개로 축약되고 `seasonRows`에 시즌별 막대 데이터가 담긴다

## 화면 동작 규칙

- **전체 탭** — 선수 1명 = 카드 1장. 시즌 중 이적자는 한 장 안에 구간별 소제목으로 나뉘고 맨 아래 "시즌 합계"가 붙는다.
- **리그 탭** — 그 리그 구간을 가진 선수만 나오고, **그 구간의 기록만** 집계된다. 이적자는 뛴 리그 탭마다 모두 등장한다.
- **의도된 불일치**: 전체 탭의 리그 섹션 인원수와 리그 탭 버튼의 인원수는 다를 수 있다. 전체 탭에서는 카드가 `primaryLeague` 아래 한 번만 놓이고, 탭 버튼은 "그 리그에서 뛴 적 있는 선수"를 전부 센다. 버그가 아니다.
- 시즌을 바꿨을 때 현재 리그 탭에 아무도 없으면 `render()`가 `curTab`을 `'all'`로 되돌린다.
- **단일 시즌 모드**는 카드가 `primaryLeague` 순(UEFA 순위 순)으로 묶이고 리그 섹션 헤더(`.slbl`)가 붙는다. **기간 합산 모드**(`mode==='range'`)는 리그 헤더 없이 **이름 가나다순**으로 평탄하게 나열된다(2026년 8월 18일 변경). 리그 탭을 눌러 필터링하는 기능은 두 모드 다 동일하게 작동 — 이 차이는 `renderCards()` 안의 표시(그룹핑/정렬) 로직에만 있고, `getPlayerData`/`stintsOf` 등 필터링 자체는 안 건드렸다.
- 대회별 표(`.comp-table`)는 `table-layout:fixed`로 출전/득점/어시/G+A 4개 숫자 컬럼 폭이 고정(58px)돼 있다. 카드마다 대회명·팀명 길이가 달라도 숫자 컬럼이 페이지 전체에서 같은 x축에 정렬된다.

## 데이터 정확성 규칙

기록을 추가·수정할 때 반드시 지킨다. 아래는 실제로 틀렸던 사례에서 나온 규칙이다.

- **통계 사이트의 다중 시즌 표를 한 번에 요약해 읽지 말 것.** 행과 열이 섞인다. 실제로 유로파리그 기록이 인접 시즌으로 옮겨간 사례(오현규 24/25에 25/26 유로파리그가 들어감)와 경고 횟수가 도움 수로 읽힌 사례(조진호)가 있었다. **시즌별 페이지를 따로** 볼 것.
- **대회별 수치의 합이 출처가 명시한 시즌 총계와 맞는지 대조한다.** 안 맞으면 그 출처를 버린다. 자체 합계가 맞아떨어지는 출처가 가장 신뢰할 만하다.
- **출처가 엇갈리면 한쪽을 임의로 고르지 말고 제3의 출처로 확정한다.** 나무위키를 근거로 Soccerway 값을 덮었다가 틀린 적이 있다(홍현석 도움 0 → 1).
- 진행 중 시즌은 각 수치가 **몇 라운드까지인지** 남긴다.
- 주 출처: Soccerway(대회별 표), Transfermarkt(교차검증), 나무위키 시즌별 문서(합계 대조용).

## 검증 방법

브라우저를 열지 않고 Node로 로직을 돌려 탭별 수치를 전부 확인할 수 있다. **2026년 8월 20일부로 스크립트 태그에 `id="mainscript"`가 붙어있고 `PLAYERS`가 하드코딩이 아니라 Supabase에서 온다** — 아래처럼 뽑아야 한다.

```js
const fs=require('fs');
const scriptText = fs.readFileSync('koreaFootball.html','utf-8')
  .match(/<script id="mainscript">([\s\S]*?)<\/script>/)[1];
// PLAYERS는 로그인/편집 UI 관련 함수(sb.auth...)까지 그대로 남겨두고,
// 자동 부트스트랩 줄(document.getElementById('content').innerHTML=...; loadPlayers().then(render)...)만
// 잘라내거나 그대로 둔 채 로 아래 DOM·localStorage 스텁 뒤에 붙여 실행한다.
```

Node 18+는 전역 `fetch`가 있으므로 `loadPlayers()`를 목업하지 않고 **실제 Supabase 프로젝트에 그대로 호출**해서 검증할 수 있다(테스트가 곧 진짜 연동 확인이 됨) — `global.document={getElementById:..., querySelectorAll:()=>[], documentElement:{...}}`, `global.localStorage={getItem:()=>null,setItem(){}}`, `global.window={addEventListener(){}}` 스텁을 두고, `loadPlayers().then(()=>{ curTab='all';mode='single';selS=sid; buildLeagueTabs(); renderCards(); ... })`처럼 실행한다. `mode`/`selS`/`curTab`을 직접 바꿔가며 여러 시즌·탭을 순회 검증.

### 웹 검색 시 언어 선택 (2026년 8월 17일 — 이현주 1라운드 출전 확인 사례)

**골·도움·카드 등 "임팩트 있는" 출전은 한글 검색으로 잘 잡히지만, 별다른 사건 없이 뛰기만 한 경기는 한글 검색에 안 걸릴 수 있다.** 한국 스포츠 매체는 자국 선수가 눈에 띄는 활약을 했을 때만 기사를 쓰기 때문에, "그냥 풀타임/일부 출전"은 애초에 관련 한글 기사 자체가 존재하지 않는 경우가 많다.

- 실제 사례: 이현주가 26/27 시즌 1라운드(8/8, 비토리아 기마랑이스전)에 선발로 70분을 뛰었지만 공격포인트가 없었다. 한글로 "이현주 1라운드", "이현주 8월 8일" 등을 아무리 검색해도 이 경기 대신 자꾸 임팩트가 있었던 작년(25/26) 데뷔전 도움 기사만 잡혔다.
- 영어로 `[팀명] [상대팀] [선수 영문명] lineup [월 연도]` 식으로 바꿔 검색하자 whoscored·playerstats.football 같은 해외 경기 데이터 사이트가 걸렸고, 여기서 실제 출전 여부(선발/교체 시간)를 확인할 수 있었다. 이런 사이트는 "기사거리가 되냐"와 무관하게 모든 선수의 출전을 기계적으로 기록한다.
- **규칙**: 특정 선수의 특정 경기 출전 여부만 확인하고 싶은데(공격포인트 유무와 무관하게) 한글 검색이 비어 있거나 자꾸 다른 시즌 기사만 나오면, 곧장 영어 검색으로 전환할 것. 반대로 이미 공격포인트가 있었다는 걸 아는 경기는 한글 검색이 더 빠르고 정확하다(다수 매체 교차검증 가능).

확인 항목:

- 전체 탭의 선수 수 = 카드 수 (이적자 중복 계상 없음)
- 시즌 중 이적자가 관련 리그 탭 **모두**에 등장
- 각 리그 탭 합계에 다른 리그 구간이 섞이지 않음
- 계수 탭 배지가 현재 시즌을 셈
- 손대지 않은 과거 시즌 수치가 변하지 않았는지 대조

## 26/27 진행 상황

**1단계(시즌 축 추가) 완료 — 2026년 8월 17일.** 이후 단계는 아래 참조.

- `SEASONS`가 6개가 됐고 `CUR='2627'`, `ASOF` 상수가 추가됨
- 선수 12명 전원 `'2627':null` — 명단 미확정 상태
- ~~26/27 버튼은 cur, 기본 선택은 25/26~~ → **2026년 8월 18일부로 기본 선택을 26/27로 전환.** 이현주·이한범·황인범에 실전 기록이 들어오면서 "데이터 비어있는 시즌을 기본 화면으로 두지 않는다"는 이유가 해소됨. `let mode=...` 줄의 `selS`·`rto`를 `CUR`로, 시즌 버튼 행의 `on` 클래스를 26/27로, `to` 셀렉트의 `selected`도 26/27로 옮겼다. 26/27 버튼은 `cur`(현재 시즌)와 `on`(선택됨)이 겹쳐 `.sbtn.cur.on` 스타일(금색+진한 배경)로 표시된다
- 특정 리그 탭에 그 시즌 한국 선수가 아예 없을 때만 `ASOF[CUR]` 안내 문구가 뜬다 (예: 26/27 세리에A·에레디비지에·체코리가 탭)
- **저장 시 화면 상태 고정 이슈 실사례**: 편집 UI로 테스트하며 26/27 탭을 눌러본 채로 "파일에 저장"을 누르면, 버튼 `.on` 클래스 등 정적 HTML은 26/27처럼 보이지만 **`let selS=...` 로직 자체는 안 바뀐다** — 실제로 이 일이 발생해서(2026-08-18) `selS`가 25/26에 머물러 있는 걸 뒤늦게 발견·수정했다. 편집 UI 문서의 "알려진 한계" 그대로다
- `koreaFootball.2526final.html` = 1단계 직전 스냅샷 (25/26 검증 완료 상태)

**26/27 명단 — 확정분만 선반영 (2026년 8월 17일 기준, 8월 17일 재검증 완료)**

소속이 확정된 9명은 `comps:[]`로 넣어 소속만 표시하고 기록은 비워뒀다. 카드에는 "소속만 확정 · 기록 입력 전"이 뜨고 합계 행은 숨겨진다.

| 선수 | 26/27 | 근거 |
|---|---|---|
| 이강인 | 아틀레티코 마드리드 · 라리가 | PSG→AT마드리드 이적 확정. €40m·5년·등번호 7 |
| 김민재 | 바이에른 뮌헨 | 매각 대상 거론됐다가 8/13 잔류 확정 보도 |
| 이재성 | 마인츠 05 | 2026-02-12 재계약 |
| 정우영 | 유니온 베를린 | 2025-05-29 완전 이적(이적료 62억). **검색에 슈투트가르트로 나오는 건 그 이전 소속** |
| 옌스 카스트로프 | 묀헨글라트바흐 | 잔류. 8/6 프리시즌 어깨 부상→수술, 수개월 결장 |
| **황인범** | **FC 포르투 · 프리메이라리가** | ⚠️ **2026-07-21 공식 이적 확정 — 페예노르트 아님.** 3+1년 계약·등번호 16·이적료 €4.5m(+ 옵션 €0.5m). 리그·나라 모두 바뀌었으니 UEFA_LEAGUES 등 4곳 동기화는 불필요(프리메이라리가는 이미 TOP10에 있음). **1라운드(8/10) 벤치, 2라운드(8/16) 교체 출전 1도움 — 이미 기록 반영됨(comps:[] 아님)** |
| 오현규 | 베식타스 JK | 잔류 중. 레알 베티스 이적설(€15m) 있었으나 구단이 부인, 무산 |
| 황의조 | 알란야스포르 | 2025-08-05 2년 재계약(~2027) 서명식 완료, 세 번째 시즌 |
| **홍현석** | **마인츠 05** | ⚠️ 8/17 재검증으로 신규 확정. 헨트 임대 종료 후 마인츠 1군 복귀, 등번호 13 |

**미확정 → `null` 유지**

- **황희찬** — 울버햄프턴 챔피언십 강등 후 정리 대상, 8/15 개막전 명단 제외. 브렌트퍼드·풀럼·라치오 등 관심설 있으나 행선지 미정
- 조진호(튀르키예 2부 임대), 손흥민(LAFC) — TOP 10 밖이라 계속 `null`

**신규 진입 조사 — 2026년 8월 17일 웹서치 결과**

- **김지수(브렌트포드, 프리미어리그, CB, 등번호 36)** — 카이저슬라우테른(분데스리가2) 임대 종료(2026-06-30), 브렌트포드 1군 복귀 확인됨. 8/16 컵경기에 센터백으로 풀타임 출전. **PLAYERS 미추가 — 백업 수비수라 출전량 지켜본 뒤 반영하기로 보류.** 다음 점검 때 리그·컵 출전 여부 재확인
- **설영우(프랑크푸르트 이적설)** — 3월부터 "월드컵 후 확정" 보도 있었으나 8/15자 나무위키 문서에도 여전히 세르비아 츠르베나 즈베즈다 소속으로만 나옴. 프랑크푸르트 언급 없음 → 미완료로 판단, `null` 유지. 9/1 마감 전 재확인 필요
- 최준(신트트라위던)은 오탐(한국 선수 아님), 이승우는 전북현대(K리그) 잔류로 무관 — 더 이상 추적 불필요

**PLAYERS 자체 누락 2명 발견·추가 — 2026년 8월 17일 (사용자 제보로 시작)**

기존 12명 명단 자체에 안 잡혀있던 선수. "이름으로 검색 불가 → 목록형 문서에서 역발견"이 여기서도 그대로 재현됨.

- **이현주 추가** (`en:'Lee Hyun-ju'`, MF) — 2003-02-07생, 포항 유스→바이에른 뮌헨(2군, 레기오날리가)→베헨 비스바덴·하노버96(2.분데스리가, 둘 다 TOP10 아님)→**2025-07-23 FC 아로카(포르투갈 프리메이라리가) 구단 역대 최고 이적료(€1.5m) 이적**. 2024-11 A대표팀 첫 발탁(쿠웨이트전)까지 있는 선수인데 명단에 아예 없었음. 등번호 14. `25/26`: 리그 30경기 7골 3도움 + 타사드포르투갈 2경기 — Soccerway·나무위키·영어 위키백과 3중 교차검증(합계 32경기 일치). `26/27`: 반영 완료, apps:2/goals:0/assists:1 — **1라운드(8/8, 비토리아 기마랑이스 원정 1-0승)** 선발, 후반 70분 교체 아웃, 공격포인트 없음(골 바르베로·도움 주아라는 팀 동료 기록) / **2라운드(8/16, 모레이렌시전 4-0승)** 선발, 1도움. 1라운드 출전 세부사항(70분 교체)은 playerstats.football 검색 스니펫 단일 출처 — 직접 fetch는 403으로 막힘. 스코어(1-0)와 "개막 2연승" 흐름 자체는 여러 소스로 교차검증됨
  - `COMP_COUNTRY`에 `'타사드포르투갈':'포르투갈'` 신규 등록 (기존엔 프리메이라리가 자체만 있었고 포르투갈 자국컵이 안 잡혀있었음)
- **이한범 추가** (`en:'Lee Han-beom'`, CB) — 2002-06-17생, 190cm, FC서울 유스→2023-08 미트윌란(덴마크 수페르리가, TOP10 아님, 전 시즌 `null` 유지)→**2026-08 클럽 브뤼헤(벨기에 주필러프로리그) 이적, 3년(~2029) 계약, 이적료 최대 174억원, 등번호 3**. 2023 항저우 아시안게임 금메달로 병역 혜택. `26/27`: 1라운드(8/8, 코르트레이크전) 센터백 선발 풀타임, 3-0 승리·MOM 선정 — 복수 출처 확인돼 1경기 반영. 이후 라운드는 미확인
  - 미트윌란 시절(덴마크) 기록은 추적 리그 밖이라 전부 `null` — 구조상 문제 없음, 참고만

**PLAYERS 자체 누락 1명 추가 발견 — 2026년 8월 18일**

- **양민혁 추가** (`en:'Yang Min-hyeok'`, FW, 윙어) — 2006-04-16생, 강원FC 유스→2024-07 토트넘 홋스퍼(€4m) 이적했으나 **PL 출전은 단 한 번도 없음**(0경기), 그동안 포츠머스·코번트리(둘 다 잉글랜드 챔피언십, TOP10 아님)로 임대만 전전 — 그래서 25/26까지 전 시즌 `null`이 맞음. **2026-08-12 KVC 베스테를로(벨기에 주필러프로리그) 임대 이적 공식 발표**(완전 이적 옵션 없음, 등번호 47). `26/27`: apps:1/goals:0/assists:0 — **2라운드(8/16, KRC 헹크 원정 2-3패)가 명시적으로 "데뷔전"이라고 보도됨 → 1라운드는 자동으로 무출전 확정**(별도 검색 불필요, 출처 자체가 "데뷔전"이라 못박음). 후반 39분 교체 출전, 공격포인트 없음
  - 나무위키 "분류:한국인 유럽파 축구 선수/2025-26 시즌" 같은 카테고리 목록에서 발견 — 기존 12명 명단에 없던 선수를 찾는 표준 루트가 여기서도 그대로 재현됨. 같은 목록에서 배준호(스토크시티)·백승호(버밍엄)·엄지성(스완지시티)·전진우(옥스퍼드)는 전부 잉글랜드 챔피언십(TOP10 아님), 윤도영은 네덜란드 2부(도르드레흐트), 양현준은 스코틀랜드(셀틱, TOP10 아님)로 확인 — 전부 무관
  - 설영우(프랑크푸르트 이적설)·김지수(브렌트포드)는 이전 점검과 상태 동일, 재확인 불필요

**과거 시즌 누락 전수조사 — 2026년 8월 17일**

기존 12명의 과거 시즌 중 TOP10 리그 재적 기간이 빠진 곳이 없는지 전수 점검. `null`인 시즌마다 그 시기 실제 소속을 확인했다.

- **황의조 2021-22 발견·추가** — 지롱댕 드 보르도(리그앙) 32경기 11골 2도움 + 쿠프드프랑스 1경기, 아시아인 최초 리그앙 해트트릭 시즌인데 `null`로 비어있었음. 나무위키·검색 교차검증(합계 33경기 11골 일치)
- **황의조 2023-24 발견·추가** — 시즌 중 소속이 노팅엄 포레스트(PL, 0경기)→노리치시티(챔피언십, TOP10 아님, 제외)→**알란야스포르 임대(2/6~, 쉬페르리그 8경기 1골)**로 세 번 바뀜. TOP10 구간인 알란야스포르만 스틴트로 반영
- 이승우(프로시노네 세리에A 영입설 — 실제론 수원FC 잔류로 불발), 조규성(마인츠 임대설 — 실제론 덴마크 미트윌란행), 황재원(아우크스부르크설 — 실제론 대구FC 잔류), 김승규(신트트라위던설 — 확인 안 됨, 현재 FC도쿄), 백승호·정상빈(라리가/분데스리가 경력이 있으나 전부 2020-21 이전 또는 2.분데스리가/챔피언십으로 TOP10 밖) — 전부 오탐·무관 확인, 추가 불필요
- Node 시뮬레이션으로 `UEFA_LEAGUES` 키 정합성(전 시즌·전 선수) 및 `buildLeagueTabs()`/`renderCards()` 파이프라인 정상 동작 확인 완료

**개막일 재확인 (8/17 기준 — 아무도 아직 실전 기록 없음)**

- 라리가: 아틀레티코의 실제 개막전은 **8/20 말라가전**(8/16은 다른 팀 매치데이1일 뿐, 이강인 개인 데뷔전 아님)
- 분데스리가: 리그 자체는 8/28 개막. **DFB-포칼 1라운드가 8/21~24 먼저 열려** 마인츠(이재성·홍현석)·유니온 베를린(정우영)·묀헨글라트바흐(카스트로프)는 그 주에 첫 공식전 가능성. 바이에른(김민재)은 포칼 대진이 9/2(오스나브뤼크)로 늦게 잡힘
- 프리메이라리가: 황인범의 포르투 데뷔전 일정 미확인 — 다음 점검 때 확인
- 쉬페르리그: 8/14 개막했으나 베식타스·알란야스포르 맞대결은 **8/23**이라 오현규·황의조 둘 다 아직 무경기

**1라운드 전수 확인 — 2026년 8월 18일**

이현주·이한범(신규 발견)·황인범 세 명은 이미 1~2라운드 기록이 있어 반영 완료(위 참조). 나머지 확정 소속 선수 전원의 리그 개막 여부를 재확인했으나 **전부 아직 미개막** — 채울 기록 자체가 없어 `comps:[]` 그대로 유지가 맞음.

- 이강인(라리가) — 개막전 8/20 말라가전, 아직 미실시. 참고: 8/18 기사에 이강인·로메로·오르티스 3명이 라리가 선수 등록이 아직 안 됐다는 보도 있었으나 "행정 절차 지연일 뿐, 개막전 전 해결 전망"으로 낙관적 — 데이터에는 영향 없음, 다음 점검 때 실제 출전 여부만 확인하면 됨
- 김민재·이재성·정우영·카스트로프·홍현석(분데스리가) — DFB-포칼 1라운드(8/21~24)·바이에른 9/2까지 전부 미실시
- 오현규·황의조(쉬페르리그) — 베식타스-알란야스포르 맞대결 8/23, 미실시

**남은 단계**

3. 명단 확정 *(이적시장 9월 1일 마감 후)* — 위 미확정 2명 + **신규 진입 탐색**. 신규 진입은 이름으로 검색할 수 없으니 "시즌별 해외파 정리" 같은 목록형 문서에서 역으로 찾는다. 이게 가장 흔한 누락 지점이다
4. 기록 수집 — 위 정확성 규칙 적용. `comps:[]`를 실제 수치로 채운다. 8월 이적생은 구간을 하나 더 추가하면 되고 구조 변경은 불필요
6. Node 시뮬레이션 검증

**5단계 · UEFA 계수 — 완료 (2026년 8월 17일)**

**어떤 표를 쓰는지가 이 탭의 핵심 규칙이다.** 배정 경로가 두 갈래라는 점을 혼동하기 쉽다.

**(1) 기본 배정 팀 수** — 5년 합산 계수를 **2년 전** 시점에서 끊어 정한다.

| 표 | 결정하는 것 | 성격 |
|---|---|---|
| 20/21~24/25 | 26/27 기본 배정 | 이미 확정, 과거 |
| **21/22~25/26** | **27/28 기본 배정** | **← 이 화면이 쓰는 표.** 25/26 종료로 확정, 시즌 내내 불변 |
| 22/23~26/27 | 28/29 기본 배정 | 진행 중. 26/27이 거의 비어 순위가 요동침 |

**(2) 유러피언 퍼포먼스 스팟(EPS)** — **직전 한 시즌** 협회 계수 상위 2개국에 챔피언스리그 한 장씩 추가. 5년 합산이 아니라 **그 시즌 단일 계수**(협회 소속 클럽들의 계수 합 ÷ 클럽 수)로 정한다.

- 26/27 EPS: 25/26 성적으로 **잉글랜드·스페인** 획득 → 리버풀(PL 5위)·레알 베티스(라리가 5위)

즉 "계수는 항상 두 해 앞을 정한다"는 표현은 **틀렸다.** 기본 배정만 2년 전 기준이고, EPS는 직전 시즌이 바로 반영된다. 이 화면이 보여주는 건 (1)의 5년 합산 표다.

라이브 표를 쓰면 시즌 초에 벨기에가 네덜란드를 앞서고 폴란드가 체코를 밀어내는 등 **추적 대상 리그가 몇 주 만에 바뀐다.** 그래서 확정 표를 쓴다.

- 출처: [2027-28 UCL 출전권 배정표](https://en.wikipedia.org/wiki/2027%E2%80%9328_UEFA_Champions_League) (UEFA 공식 사이트·kassiesa는 403으로 막힘)
- **TOP 10 구성과 순서는 이전(4월 30일 잠정치)과 동일.** 값만 갱신: 잉글랜드 117.964→119.519, 스페인 96.734→97.046, 독일 92.331→92.902, 프랑스 82.998→83.498. 나머지 6개국은 변동 없음 — 시즌 막판까지 유럽대항전에 팀이 남아 있던 나라만 오른 것이라 정합적이다
- **주시할 것: 11위 그리스 48.412 vs 10위 체코 48.525, 격차 0.113.** 다음 갱신 때 TOP 10 경계가 바뀔 수 있고, 그러면 `UEFA_LEAGUES`·`COUNTRY_COLOR`·`COMP_COUNTRY`·`LC` 네 곳을 함께 고쳐야 한다

> 2단계(진행 중 표기)는 1단계에서 함께 처리했다. `render()`의 라벨이 `selS===CUR`이면 "진행 중"으로 분기한다.
> `renderCoef`도 `CUR` 기준으로 전환 완료 — 하드코딩된 `'2526'`은 더 이상 없다.

## 시즌 추가 절차

시즌 키는 4자리(`'2627'`), 라벨은 `'26/27'`.

**같이 고쳐야 하는 지점** — 하나만 빠뜨려도 화면이 어긋난다. 줄 번호는 26/27 축 추가 시점 기준이라 드리프트하므로 식별자로 찾는 걸 권한다.

| 위치 | 할 일 |
|---|---|
| `SEASONS` (234행) | 새 키 추가 |
| `SL` (235행) | 라벨 추가 |
| `CUR` (236행) / `ASOF` (237행) | 현재 시즌 키·기준일 문구 갱신 |
| 시즌 버튼 행 (115행) | 버튼 추가 · `cur` 클래스를 새 시즌으로 이동 |
| `from`/`to` 셀렉트 (126·129행) | option 추가 · `to`의 `selected` 이동 |
| 기본 상태값 (399행) | `selS`, `rto` 갱신 |
| `.fhint` / `mode-label` 초기값 / 각주 | 진행 중·기준일·미확정자 문구 갱신 |
| `PLAYERS` 전원 | 새 시즌 키 추가 (미확정이면 `null`) |

`PLAYERS` 전원에 키를 추가할 땐 `,seasons:{\n    '<직전시즌>':` 패턴을 전체 치환하면 12명이 한 번에 처리된다.

`renderCoef`와 모드 라벨은 `CUR`·`SL[CUR]`·`SEASONS.length`를 참조하므로 **시즌 문자열 하드코딩이 남아 있지 않다.** 새 시즌을 추가할 땐 `CUR` 한 줄만 바꾸면 계수 탭까지 따라온다.

## 미결정 사항

정해지지 않았으므로 임의로 진행하지 말고 확인할 것.

- **시즌 범위** — 26/27을 더하면 6개가 된다. 6개 유지 vs 가장 오래된 시즌 제거. (21/22에 손흥민 득점왕 시즌이 있음)
- ~~계수 탭의 성격~~ → **확정 계수로 결정. 2026년 8월 17일 적용 완료** (아래 참조)
- **TOP 10 이탈 선수** — 그냥 `null`로 빼기 vs "이탈" 표시로 흔적 남기기. 현재는 손흥민(MLS) 사례를 따라 `null` 처리 중.

## 알려진 불확실 데이터

출처가 엇갈려 확정하지 못한 값. 더 나은 출처를 찾으면 갱신할 것.

- 오현규 25/26 튀르키예컵 **1도움** — Soccerway는 0, 나무위키·Tribuna는 1. 4월 23일 알란야스포르 8강전 도움 기록이 별도로 확인돼 1로 채택
- 조진호 25/26 튀르키예컵 **1도움** — Soccerway 단일 출처. 같은 페이지의 다른 값이 오독된 전력이 있어 신뢰도 낮음
- 이강인 25/26 **FIFA 클럽월드컵 4경기 1골** — 2025년 6~7월 개최라 24/25로 보는 집계도 있다. 현재는 25/26에 포함
